# 赛博分身小智：MCP人格、记忆与情绪服务说明

更新日期：2026-09-08。

## 1. 这套服务给谁使用

本次小智云接入点属于朋友的“赛博分身小智”，定位是数字分身和聊天伙伴，不是AI宠物。

它复用AI Pet工程的服务能力，但使用独立普通用户、独立虚拟设备、独立人格档案和记忆归属。不关联现有四块宠物板子，不读取宠物的主人资料、私人记忆或约定。星座、MBTI、真实经历等未提供的信息保持空白，不自动编造。

用户已明确授权相关个人上下文向MiniMax出站，用于个性化回复。令牌、密码、Cookie不写入本文档或Git。

## 2. 工具清单

小智云数字分身入口提供14个工具。自建小智的实体板入口保留其中前10个工具，原有Memory MCP仍独立挂载。

共14个工具：原有4个（音乐、运势、文化解读、日常聊天），新增10个。下面的例子是自然语言使用场景，实际工具选择由小智结合对话完成。

| 工具 | 有什么用 | 使用例子 | 联网或存储行为 |
|---|---|---|---|
| search_netease_music | 搜索歌曲、歌手，获取播放资源 | 找一首周杰伦的歌 | 网易云音乐网关；取得URL不等于设备已播放 |
| zodiac_fortune | 生成娱乐性质的星座运势 | 聊聊处女座今天的运势 | MiniMax-M3真实搜索，再由M2.5生成 |
| metaphysics_analysis | 解读传统文化和玄学话题 | 白露有什么传统寓意？ | 真实搜索后生成；不作确定性预测或精确排盘 |
| daily_chat | 兼容原来的聊天入口，结合人格与上下文回复 | 今天想随便聊聊 | 读取独立人格及批准偏好，真实搜索后回复 |
| conversation_plan | 决定回复长短、倾听或建议比例、是否追问 | 用户疲惫时，减少追问 | 内部策略计算，不生成外部事实 |
| context_search | 检索相关记忆和公开资料，供后续回复使用 | 查点有关阅读习惯的资料 | MiniMax搜索公开主题，返回来源和短期search_id |
| companion_reply | 主要的人格化回复工具，结合档案、关系和批准偏好 | 结合我的习惯给些建议 | 真实搜索后生成，可复用同会话同主题的检索证据 |
| interaction_feedback | 学习用户明确提出的沟通偏好 | 以后少问问题，直接一点 | 保存候选偏好，审批后长期采用 |
| shared_activity | 发起故事、问答、回顾或放松等共同活动 | 陪我玩一轮趣味问答 | 联网设计活动第一步，等待用户继续 |
| followup_plan | 保存、查询、完成或取消后续约定 | 明天聊天时问问我的阅读进度 | 明确时间及同意后保存，在后续合适会话中关心 |
| memory_search | 查找已经批准的长期记忆 | 你记得我喜欢什么吗？ | 只检索当前独立身份的active记忆 |
| memory_add | 保存用户明确要求记住的内容 | 记住我喜欢周末读书 | 脱敏后保存candidate，先进入待审批状态 |
| memory_forget | 按明确要求遗忘指定记忆 | 忘掉之前那件事 | 先确定记忆ID，再软归档并记审计 |
| emotion_support | 根据当下心情提供倾听和支持 | 今天有点累，陪我聊聊 | MiniMax真实搜索；不诊断、不自动保存情绪推断 |

### 容易混淆的工具有什么区别

- conversation_plan选择“怎么说”，companion_reply负责“实际回复”。后者内部已读取策略和上下文，通常无需每轮先调用前者。
- daily_chat是旧聊天入口的兼容升级；新的人格化交流优先使用companion_reply。
- interaction_feedback记录“怎么和你说话”，例如简短、少追问；memory_add记录“你要求记住什么”，例如爱好或经历。两者都先进入候选状态。
- context_search会搜索公开资料并关联记忆；memory_search只查独立空间中已批准的长期记忆，不发起外部搜索。
- emotion_support聚焦当下感受；shared_activity邀请用户一起做一个轻量活动，不把活动建议视为已经完成。

内容生成通过MiniMax真实联网；策略计算和记忆读写不额外搜索。约定是后续会话中的跟进，不是离线准点闹钟。音乐能否实际播放取决于设备是否提供播放工具及其执行结果。

星座、MBTI策略不是确定性人格诊断，也不应改变事实答案。情绪支持优先回应用户当前表达，不使用排他性或依赖性话术。

## 3. 人格与沟通策略怎么工作

代码包含12份星座策略和16份MBTI策略，每份有表达风格、支持方式、避免事项和正反例；组合覆盖192种情况，版本为companion-20260908-v1。

策略优先级：

1. 用户明确表达且已批准的偏好。
2. 当前情境：正常、忙碌、疲惫、低落、冲突或庆祝。
3. 用户类型提供的初始沟通倾向。
4. 陪伴角色自身的表达风格。

支持调整回复长短、倾听/建议比例、追问数量及温柔/直接/轻松语气。未知类型使用中性策略，不能因为星座或MBTI猜测真实经历、能力或内心。

数字分身已建立独立基础档案：平等、真诚的数字分身与聊天伙伴。朋友的具体人格设定仍应由本人提供并维护。

## 4. MiniMax联网与记忆边界

内容生成采用MiniMax-M3执行服务端web_search，再交给MiniMax-M2.5生成中文最终回复。成功结果包含搜索执行标记、模型、来源与检索时间；没有有效搜索证据时返回失败，不能退回普通模型生成并声称已联网。

公开搜索只发送公开主题，姓名、联系方式、私人经历应作为回复上下文处理，不拼入公开检索词。用户已授权的人格、关系及相关记忆可发送给MiniMax用于组织个性化回复。

context_search的证据缓存最多128条、有效180秒，只允许同设备、同会话、同主题复用，不接受模型自行提供的“搜索证据”。

策略计算、记忆读写和约定管理属于内部规则/数据操作，不需要额外搜索；歌曲资源由网易云提供。

## 5. 记忆、反馈和约定

例如用户说“以后回复简短一点”，interaction_feedback保存候选偏好；用户通过独立账号在App审批后，后续策略才采用。未审批的偏好不会通过普通记忆召回绕过审批。

例如用户说“记住我喜欢周末读书”，memory_add只创建候选。memory_search仅检索active；明确要求遗忘时才调用memory_forget归档。

followup_plan需要明确的事项和带时区的时间。时间必须在未来90天内，同一请求号用于去重；约定到期后保留7天提示窗口。在东八区08:00–21:00的合适会话中可关心用户，不承诺离线设备准点唤醒。已取消和已完成不能相互覆盖；提示过不等于事项完成。

## 6. 接入与身份隔离

云端链路：

小智云接入点 → 独立WebSocket桥接容器 → stdio MCP → 内容/人格工具、业务后端和Memory MCP。

私有配置固定数字分身身份cloud-agent-2323485，桥接重连时生成新会话ID。模型工具参数不能选择设备、用户、会话或内部令牌。

自建小智仍使用HTTP MCP。设备身份由已认证连接注入，首次连接和重连都覆盖静态身份头。四块已登记实体板的服务端接入与重连已验证，未在本次烧录固件或验证扬声器实播。

小智云接入点只负责工具通道，不等于固件OTA/实时语音WebSocket地址。不要把MCP接入令牌写入固件仓库。

## 7. 部署位置与代码入口

服务位于39.107.143.71。

| 内容 | 位置 |
|---|---|
| 音乐与内容服务 | /opt/xiaozhi-music-aipet |
| 云端接入点与固定身份配置 | /opt/xiaozhi-music-aipet/cloud.env，权限0600 |
| 数字分身独立账号凭据 | /opt/xiaozhi-music-aipet/cloud-account.json，权限0600 |
| MiniMax密钥配置 | /opt/xiaozhi-music-aipet/content.env |
| 音乐私有配置 | /opt/xiaozhi-music-aipet/music.env |
| 业务后端 | /opt/ai-pet/ai-pet-backend |
| 自建小智 | /opt/xiaozhi-server |
| 本次发布备份与检查日志 | /opt/aipet-companion-yCjfbMYD |
| 本地内容/策略源码 | D:/xiaozhi_music_mcp/server |
| 发布与验证脚本 | D:/Home_Work/xiaozhi-server/deploy/music-aipet |

关键源码：content_tools.py（联网生成）、personality_policies.py（28份策略）、companion_tools.py（六个陪伴工具）、cloud_bridge.py（云端桥接）、cloud_entry.py（云端记忆与情绪工具）。

云端工具成功返回后应自然播报最终内容，不朗读策略JSON、令牌或长链接。可在小智云角色提示中说明：日常聊天优先使用companion_reply，情绪支持使用emotion_support，需要记住/回忆/遗忘时分别调用记忆工具；失败如实告知。

## 8. 验证状态与使用检查

- 后端ruff、mypy及139项测试通过。
- 音乐/内容/陪伴43项本地测试通过，覆盖192种策略组合与身份、缓存隔离。
- 独立PostgreSQL迁移、真实HTTP审批、换绑隔离、脱敏、候选记忆及MCP约定写入验收通过。
- 自建小智四块实体板作用域、原生客户端重连、系统提示开关验证通过。
- 线上检索复用、活动、聊天、运势、文化解读已通过真实MiniMax调用，本轮约16–29秒并返回来源。
- 云端同款stdio入口14工具、独立记忆读取和情绪支持已验证。
- 小智云WebSocket正式注册已通过：2026-09-08 20:14（北京时间）连接成功，收到initialize与tools/list，桥接向云端返回14个工具。后台可点击接入点刷新查看。
- 硬件播放、朋友实际语音体验及本人设定内容仍需实际使用验收。

如果小智云后台仍只显示zodiac_fortune、metaphysics_analysis、daily_chat、search_netease_music四项，说明看到的仍是旧服务工具清单。应先检查新桥接容器的注册日志，再点击接入点刷新；只有出现cloud tools registered count=14，才可认定云端已收到本桥接的全部工具。

## 9. 后续维护

代码修改按既有授权执行验证、构建、部署、提交推送并更新协作看板。纯文档修改不重启服务。本地两个目录中的本文档保持同步，不复制密钥。

当前音乐源工程未配置Git远端，只能本地提交；有origin的工作仓和本板卡仓按各自远端提交。发布回滚使用备份配置和旧镜像；新增约定表保留，避免丢失已接受的数据。
